Upload the new image to Lovable Assets as `src/assets/yzol-banner.png.asset.json`, then update `src/pages/brands/YzolLubricantsPage.tsx` to import the new pointer and use its `.url` for the hero `<img src>` (replacing the current `yzol-banner.jpg` import). Delete the old `yzol-banner.jpg` import/file if no longer referenced.