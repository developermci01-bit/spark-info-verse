Add the user-supplied Tawk.to script snippet to `index.html` just before the closing `</body>` tag so the chat widget loads on every route of the React SPA.

No other files need to change. The script is self-contained and will initialize on page load.